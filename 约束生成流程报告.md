# Demo3 约束生成流程详细报告

## 一、整体流程概览

Demo3 中的约束生成流程位于 **Step 3**，采用 **Action-aware Constraint Generation（动作感知约束生成）** 方法。主要特点是：为每个动作生成对应的 Precondition 和 Postcondition 约束，并将约束精确绑定到动作序列中的具体步骤。

## 二、代码位置

- **Notebook Cell**: Cell 24 (Step 3)
- **核心模块**: 
  - `craft/reasoning/constraint_generator.py` - ConstraintGenerator 类
  - `craft/reasoning/llm_prompter.py` - LLMPrompter 类

---

## 三、详细流程步骤

### 3.1 初始化和检查（Cell 24, 第1-44行）

**功能**: 检查 CRAFT 模块和 LLM Prompter 是否可用，如果不可用则尝试重新初始化。

```python
# 1. 检查 CRAFT_AVAILABLE
try:
    craft_available = CRAFT_AVAILABLE
except NameError:
    craft_available = False

# 2. 检查 llm_prompter 是否可用
try:
    llm_prompter_exists = llm_prompter is not None
except NameError:
    llm_prompter_exists = False

# 3. 如果不可用，尝试重新初始化
if craft_available and not llm_prompter_exists:
    # 获取 API 配置（从全局变量或使用默认值）
    # 创建新的 LLMPrompter 实例
```

**关键点**:
- 如果 `CRAFT_AVAILABLE` 为 False 或 `llm_prompter` 不可用，则跳过约束生成
- 支持自动重新初始化 LLM Prompter，提高容错性

---

### 3.2 模块加载和热重载（Cell 24, 第50-59行）

**功能**: 加载必要的模块，支持开发时的热重载。

```python
import importlib
import sys

# 如果需要重新加载模块（用于开发时的热重载）
if 'craft.reasoning.llm_prompter' in sys.modules:
    importlib.reload(sys.modules['craft.reasoning.llm_prompter'])
if 'craft.reasoning.constraint_generator' in sys.modules:
    importlib.reload(sys.modules['craft.reasoning.constraint_generator'])

from craft.reasoning import LLMPrompter, ConstraintGenerator
```

**关键点**:
- 使用 `sys.modules` 安全地重新加载模块，避免导入错误
- 确保使用最新版本的代码

---

### 3.3 LLM Prompter 重新实例化（Cell 24, 第61-84行）

**功能**: 重新创建 LLM Prompter 实例以加载最新的 Action-aware Prompt 模板。

```python
# 尝试从旧的 prompter 获取配置
try:
    old_prompter = llm_prompter
    llm_prompter = LLMPrompter(
        gpt_version=old_prompter.gpt_version,
        api_key=old_prompter.api_key,
        base_url=old_prompter.base_url
    )
except Exception:
    # 如果失败，使用全局配置重新创建
    llm_prompter = LLMPrompter(
        gpt_version=gpt_version,
        api_key=api_key,
        base_url=base_url
    )
```

---

### 3.4 创建 ConstraintGenerator（Cell 24, 第86行）

```python
constraint_generator = ConstraintGenerator(llm_prompter)
```

**ConstraintGenerator 初始化**:
- 接收一个 `LLMPrompter` 实例作为参数
- 内部保存 `self.llm_prompter` 用于后续 LLM 查询

---

### 3.5 为每个任务生成约束（Cell 24, 第88-103行）

**循环遍历每个任务**:
```python
for task_name, data in task_data.items():
    if 'final_sg' not in data:
        continue
    
    task_info = data['task_info']
    final_sg = data['final_sg']  # 最终场景图
    actions = task_info.get('actions', [])  # 动作序列
    
    # 调用 LLM 进行约束生成
    constraints = constraint_generator.generate_constraints(
        scene_graph=final_sg,
        task_info=task_info,
        goal=task_info.get('success_condition', '')
    )
```

**输入数据**:
- `final_sg`: 任务执行完成后的场景图（SceneGraph 对象）
- `task_info`: 任务信息字典，包含：
  - `name`: 任务名称
  - `actions`: 动作序列列表，例如 `["(pick_up, Mug)", "(put_in, Mug, CoffeeMachine)"]`
  - `success_condition`: 成功条件描述

---

## 四、核心方法：generate_constraints

### 4.1 方法签名（constraint_generator.py, 第190-191行）

```python
def generate_constraints(self, scene_graph: SceneGraph, task_info: Dict, 
                        goal: Optional[str] = None) -> List[Dict]:
```

**参数**:
- `scene_graph`: 当前场景图（SceneGraph 对象）
- `task_info`: 任务信息字典
- `goal`: 可选的目标描述

**返回值**:
- `List[Dict]`: 约束字典列表，每个约束包含：
  - `id`: 约束 ID
  - `type`: 约束类型（precondition/postcondition）
  - `template`: 模板表达式
  - `description`: 人类可读的描述
  - `condition_expr`: 可执行的条件表达式
  - `action`: 关联的动作字符串

---

### 4.2 构建 Prompt（constraint_generator.py, 第203-236行）

**步骤 1: 提取场景图和任务信息**
```python
scene_text = scene_graph.to_text()  # 将场景图转换为文本描述
task_name = task_info.get('name', '')
goal_text = goal or task_info.get('success_condition', '')
actions_text = ", ".join(task_info.get('actions', []))  # 动作序列转字符串
```

**步骤 2: 获取 Prompt 模板**
```python
prompt_info = self.llm_prompter.prompts['constraint-generator']
template_user = prompt_info['template-user']  # 用户 prompt 模板
template_system = prompt_info['template-system']  # 系统 prompt 模板
```

**步骤 3: 格式化 Prompt**
```python
user_prompt = template_user.format(
    task=task_name,
    actions=actions_text,
    scene_graph=scene_text,
    goal=goal_text
)
```

---

### 4.3 Prompt 模板详解（llm_prompter.py, 第39-89行）

**System Prompt**:
```
You are a robot task analyzer. Generate structured logical constraints by 
instantiating predefined templates for EVERY action in the sequence.
```

**User Prompt 模板**:
```python
'''Task: {task}
Action Sequence: {actions}
Scene Graph: {scene_graph}
Task Goal: {goal}

You must generate a COMPREHENSIVE set of constraints covering the ENTIRE 
execution flow by instantiating the following ACTION SEMANTIC TEMPLATES 
for each action:

1. pick_up(X):
   - Pre: reachable(X), gripper_empty
   - Post: holding(X)
2. put_on(X, Y):
   - Pre: holding(X)
   - Post: is_on(X, Y)
3. put_in(X, Y):
   - Pre: holding(X), container_open(Y), container_empty(Y)
   - Post: inside(X, Y)
4. toggle_on(Y):
   - Pre: reachable(Y), holding(X)
   - Post: toggled(Y)
5. toggle_off(Y):
   - Pre: toggled(Y)
   - Post: not toggled(Y)

Rules:
- For EACH physical manipulation in the Action Sequence, you MUST instantiate 
  the corresponding Pre and Post templates.
- **Holding Rule (CRITICAL)**: For every "put_on", "put_in", or "toggle" action, 
  you MUST explicitly include a "holding(X)" precondition with the EXACT 
  description "Robot must be holding the X".
- Replace X and Y with the SPECIFIC object names from the action or scene graph.
- Identify Preconditions (type: "pre") and Postconditions (type: "post").

Output the constraints in this JSON format:
{
  "constraints": [
    {
      "id": "C1",
      "template": "holding(Pot)",
      "type": "pre",
      "action": "put_on(Pot, StoveBurner-4)",
      "description": "Robot must be holding the pot",
      "severity": "hard"
    },
    ...
  ]
}

Return ONLY valid JSON.'''
```

**关键特性**:
1. **动作语义模板**: 为常见动作（pick_up, put_in, put_on, toggle_on 等）定义了标准的 Precondition 和 Postcondition 模板
2. **强制规则**: 特别强调对于 "put_on", "put_in", "toggle" 动作，必须包含 "holding(X)" precondition
3. **对象实例化**: 要求将模板中的 X, Y 替换为场景图中的具体对象名称
4. **JSON 格式输出**: 要求 LLM 返回结构化的 JSON 格式

---

### 4.4 调用 LLM（constraint_generator.py, 第246-250行）

```python
response, _ = self.llm_prompter.query(
    prompt_info['template-system'],
    user_prompt,
    max_tokens=2000  # 增加 token 数量以支持 JSON 输出
)
```

**LLMPrompter.query 方法**（llm_prompter.py, 第101-149行）:
- 使用 OpenAI API（或兼容的 API，如 poloapi）调用 LLM
- 支持自定义 `base_url`（用于使用第三方 API 服务）
- 返回响应文本和元数据（token 使用量等）

---

### 4.5 解析 LLM 响应（constraint_generator.py, 第253行和第257-372行）

**主要解析方法**: `_parse_constraints(llm_response: str) -> List[Dict]`

**步骤 1: 尝试解析为 JSON**
```python
# 提取 JSON（处理 markdown 代码块）
response_text = llm_response.strip()
if '```json' in response_text:
    response_text = response_text.split('```json')[1].split('```')[0].strip()
elif '```' in response_text:
    response_text = response_text.split('```')[1].split('```')[0].strip()

# 解析 JSON
data = json.loads(response_text)
if 'constraints' in data:
    for constraint in data['constraints']:
        # 规范化约束类型
        raw_type = str(constraint.get('type', 'precondition')).lower()
        if any(kw in raw_type for kw in ['pre', 'before']):
            constraint_type = 'precondition'
        elif any(kw in raw_type for kw in ['post', 'after']):
            constraint_type = 'postcondition'
        
        # 构建约束字典
        constraints.append({
            'id': str(constraint.get('id', f'C{len(constraints)+1}')),
            'type': constraint_type,
            'template': str(template_val),
            'action': str(constraint.get('action', '')),
            'description': str(constraint.get('description', '')),
            'condition_expr': str(constraint.get('condition_expr') or template_val),
            'severity': str(constraint.get('severity', 'hard')),
            'action_index': constraint.get('action_index'),  # 如果 LLM 提供
        })
```

**步骤 2: 回退到文本解析**（如果 JSON 解析失败）
- 按行解析文本
- 使用关键词匹配推断约束类型（precondition/postcondition）
- 从描述中提取条件表达式

**解析后的约束结构**:
```python
{
    'id': 'C1',
    'type': 'precondition',  # 或 'postcondition'
    'template': 'holding(Pot)',
    'action': 'put_on(Pot, StoveBurner-4)',
    'description': 'Robot must be holding the pot',
    'condition_expr': 'holding(Pot)',
    'severity': 'hard',
    'action_index': None  # 将在后续步骤中绑定
}
```

---

## 五、动作绑定（Action Binding）

### 5.1 绑定流程（Cell 24, 第105-152行）

**功能**: 将生成的约束精确绑定到动作序列中的具体步骤。

```python
action_bound_constraints = []
for constraint in constraints:
    raw_desc = constraint.get('description', '')
    template = str(constraint.get('template') or 'N/A')
    llm_action_str = str(constraint.get('action', '')).lower()
    
    bound_action = None
    action_index = None
    
    # A. 优先尝试 LLM 提供的动作字符串匹配
    if llm_action_str and llm_action_str != 'n/a':
        clean_llm_action = llm_action_str.split(':')[-1].strip()
        for idx, act in enumerate(actions):
            if clean_llm_action in act.lower() or act.lower() in clean_llm_action:
                bound_action = act
                action_index = idx
                break
    
    # B. 语义绑定（基于模板对象和动作关键词）
    if action_index is None:
        import re
        # 从模板中提取对象名称
        objs_in_template = re.findall(r'\((.*?)\)', template)
        objs_to_match = [p.strip().lower() for p in objs_in_template[0].split(',')] 
            if objs_in_template else []
        
        # 从描述中提取对象名称（大写开头的单词）
        desc_objs = [o.lower() for o in re.findall(r'\b([A-Z][a-zA-Z0-9]+)\b', raw_desc)]
        objs_to_match = list(set([o for o in objs_to_match + desc_objs if len(o) > 2]))
        
        # 在动作序列中查找匹配的动作
        for idx, act in enumerate(actions):
            act_lower = act.lower()
            if any(obj in act_lower for obj in objs_to_match):
                # 基于模板类型进行更精确的匹配
                is_type_match = False
                is_navigate = 'navigate' in act_lower
                
                if 'reachable' in template.lower():
                    is_type_match = is_navigate
                elif 'holding' in template.lower():
                    is_type_match = not is_navigate and not any(
                        kw in act_lower for kw in ['pick', 'grab', 'take'])
                else:
                    is_type_match = not is_navigate
                
                if is_type_match:
                    bound_action = act
                    action_index = idx
                    break
    
    constraint['bound_action'] = bound_action
    constraint['action_index'] = action_index
    action_bound_constraints.append(constraint)
```

**绑定策略**:

1. **优先策略（A）**: LLM 提供的动作字符串匹配
   - 如果 LLM 在约束中提供了 `action` 字段，直接匹配动作序列
   - 使用字符串包含关系进行匹配（不区分大小写）

2. **回退策略（B）**: 语义绑定
   - 从约束模板中提取对象名称（如 `holding(Pot)` → `Pot`）
   - 从描述中提取对象名称（大写开头的单词）
   - 在动作序列中查找包含这些对象名称的动作
   - 根据模板类型（如 `reachable`, `holding`）进行更精确的匹配

**绑定结果**:
```python
constraint['bound_action'] = "(put_on, Pot, StoveBurner-4)"  # 绑定的动作
constraint['action_index'] = 7  # 动作在序列中的索引（从0开始）
```

---

## 六、约束输出格式

### 6.1 输出结构

```python
{
    'id': 'C1',                                    # 约束 ID
    'type': 'precondition',                        # 约束类型
    'template': 'holding(Pot)',                    # 模板表达式
    'action': 'put_on(Pot, StoveBurner-4)',       # 关联的动作字符串
    'description': 'Robot must be holding the pot', # 人类可读的描述
    'condition_expr': 'holding(Pot)',              # 可执行的条件表达式
    'severity': 'hard',                            # 严重程度
    'bound_action': '(put_on, Pot, StoveBurner-4)', # 绑定的动作（绑定后）
    'action_index': 7                              # 动作索引（绑定后）
}
```

### 6.2 显示格式（Cell 24, 第156-167行）

```python
for i, c in enumerate(action_bound_constraints, 1):
    c_type = str(c.get('type', '')).lower()
    type_label = "[PRE]" if 'pre' in c_type else "[POST]"
    
    description = c.get('description', c.get('template', 'N/A'))
    idx = c['action_index']
    step_str = f"Step {idx+1}" if idx is not None else "Step ?"
    
    print(f"      {i}. {type_label} {description}")
    print(f"         -> Action: {c['bound_action']} ({step_str})")
```

**输出示例**:
```
   1. [PRE] Robot must be holding the pot
      -> Action: (put_on, Pot, StoveBurner-4) (Step 8)
   2. [POST] Pot is on top of StoveBurner-4
      -> Action: (put_on, Pot, StoveBurner-4) (Step 8)
```

---

## 七、关键设计特点

### 7.1 Action-aware（动作感知）

- **为每个动作生成约束**: 不仅生成任务级别的约束，还为每个动作生成对应的 Precondition 和 Postcondition
- **精确绑定**: 通过动作绑定机制，将约束精确关联到动作序列中的具体步骤
- **支持动作级验证**: 可以在每个动作执行前后检查对应的约束

### 7.2 模板化生成

- **动作语义模板**: 为常见动作定义了标准的模板（如 pick_up, put_in, put_on）
- **实例化**: LLM 将模板中的占位符（X, Y）替换为具体对象名称
- **可扩展**: 可以轻松添加新的动作模板

### 7.3 结构化输出

- **JSON 格式**: 要求 LLM 返回结构化的 JSON，便于解析
- **容错解析**: 如果 JSON 解析失败，回退到文本解析
- **规范化**: 自动规范化约束类型（precondition/postcondition）

### 7.4 智能绑定

- **多策略匹配**: 优先使用 LLM 提供的动作信息，回退到语义匹配
- **对象匹配**: 通过对象名称匹配动作
- **类型感知**: 根据模板类型（如 holding, reachable）进行更精确的匹配

---

## 八、数据流图

```
输入数据
  ├─ final_sg (最终场景图)
  ├─ task_info (任务信息)
  │   ├─ name
  │   ├─ actions (动作序列)
  │   └─ success_condition
  └─ goal

      ↓
  
generate_constraints()
  ├─ 提取场景图文本 (scene_graph.to_text())
  ├─ 构建 Prompt
  │   ├─ task name
  │   ├─ actions (转为字符串)
  │   ├─ scene graph (文本)
  │   └─ goal
  │
  ├─ 调用 LLM (llm_prompter.query())
  │   ├─ System Prompt (角色定义)
  │   └─ User Prompt (任务描述 + 模板规则)
  │
  └─ 解析 LLM 响应
      ├─ JSON 解析（优先）
      └─ 文本解析（回退）

      ↓

约束列表 (List[Dict])
  ├─ id
  ├─ type (precondition/postcondition)
  ├─ template
  ├─ description
  ├─ condition_expr
  ├─ action
  └─ severity

      ↓

动作绑定 (Action Binding)
  ├─ 策略 A: LLM 提供的动作匹配
  └─ 策略 B: 语义绑定（对象名称匹配）

      ↓

绑定后的约束 (action_bound_constraints)
  ├─ 原有字段
  ├─ bound_action (绑定的动作)
  └─ action_index (动作索引)

      ↓

存储到 task_data
  └─ data['constraints'] = action_bound_constraints
```

---

## 九、代码文件位置总结

| 组件 | 文件路径 | 关键类/函数 |
|------|---------|------------|
| Notebook Step 3 | `demo3.ipynb` (Cell 24) | 主流程代码 |
| 约束生成器 | `craft/reasoning/constraint_generator.py` | `ConstraintGenerator.generate_constraints()` |
| LLM 接口 | `craft/reasoning/llm_prompter.py` | `LLMPrompter.query()` |
| Prompt 模板 | `craft/reasoning/llm_prompter.py` (第39-89行) | `prompts['constraint-generator']` |
| 场景图 | `craft/core/scene_graph.py` | `SceneGraph.to_text()` |

---

## 十、使用示例

### 输入数据示例

```python
task_data = {
    'makeCoffee': {
        'task_info': {
            'name': 'makeCoffee',
            'actions': [
                '(pick_up, Mug)',
                '(put_in, Mug, CoffeeMachine)',
                '(toggle_on, CoffeeMachine)'
            ],
            'success_condition': 'Mug is filled with coffee'
        },
        'final_sg': <SceneGraph object>
    }
}
```

### 输出约束示例

```python
[
    {
        'id': 'C1',
        'type': 'precondition',
        'template': 'gripper_empty',
        'description': 'Robot gripper must be empty',
        'condition_expr': 'gripper_empty',
        'action': 'pick_up(Mug)',
        'bound_action': '(pick_up, Mug)',
        'action_index': 0
    },
    {
        'id': 'C2',
        'type': 'postcondition',
        'template': 'holding(Mug)',
        'description': 'Robot must be holding the Mug',
        'condition_expr': 'holding(Mug)',
        'action': 'pick_up(Mug)',
        'bound_action': '(pick_up, Mug)',
        'action_index': 0
    },
    {
        'id': 'C3',
        'type': 'precondition',
        'template': 'holding(Mug)',
        'description': 'Robot must be holding the Mug',
        'condition_expr': 'holding(Mug)',
        'action': 'put_in(Mug, CoffeeMachine)',
        'bound_action': '(put_in, Mug, CoffeeMachine)',
        'action_index': 1
    },
    # ... 更多约束
]
```

---

## 十一、当前实现的问题和改进方向

### 11.1 当前实现存在的问题（基于 improve4.md 分析）

虽然当前的约束生成流程在语义模板设计和输出格式方面是正确的，但存在以下系统性错误：

#### ❌ 问题 1：使用 final_scene_graph 生成所有动作约束

**当前行为**:
```python
generate_constraints(scene_graph=final_sg, actions=full_action_list)
```

**问题**:
- `final_sg` 是任务完成后的"未来世界"
- LLM 在生成第 1～N 步动作的约束时不可避免"偷看未来"
- 导致 `pick_up` 绑定了最终 on/inside 状态，`put_in` 绑定了 toggle_on/toggle_off 的结果
- 前后置条件严重错位

**本质问题**: 约束生成违反了因果顺序（causality violation）

#### ❌ 问题 2：约束生成是"任务级"，但目标是"动作级"

**当前设计**:
- 一次 Prompt 覆盖整个任务 + 全动作序列
- LLM 被要求生成 ENTIRE execution flow 的约束

**问题**:
- LLM 会自动补全跨动作的因果链
- 无法保证每条约束只对应一个原子动作
- Action Binding 逻辑变得极其复杂且不稳定

#### ❌ 问题 3：Action Binding 是"事后修补"，而非"生成即绑定"

**当前方式**:
- LLM 生成 → 再用字符串/对象/语义匹配回绑动作

**问题**:
- Binding 逻辑本身不可靠
- 越复杂越难保证正确性

**本质**: 如果约束在生成时就知道它属于哪个 action，就不需要复杂绑定

---

### 11.2 改进方向（Action-centric Constraint Instantiation）

#### ✅ 核心改动思想

从 **"Task-level constraint generation"** → 转为 **"Action-centric constraint instantiation"**

#### ✅ 优化方案 1：改为「按动作生成约束」（最重要）

**❌ 当前**:
```
Input: final_scene_graph + full action list
Output: all constraints
```

**✅ 改为**:
```python
For each action a_i:
    Input:
        - 当前 action a_i
        - action type (pick_up / put_in / ...)
        - minimal object context（可选）
    Output:
        - Pre(a_i)
        - Post(a_i)
```

**效果**:
- 每条约束天然绑定到 `action_index = i`
- 不再需要复杂 Action Binding

#### ✅ 优化方案 2：Prompt 改为 Action-local（不再全任务）

**Prompt 输入应只包含**:
```
Current Action: (put_in, Mug, CoffeeMachine)
Action Index: 9
Action Semantic Template:
    - Pre: holding(Mug), container_open(CoffeeMachine), container_empty(CoffeeMachine)
    - Post: inside(Mug, CoffeeMachine)
```

**明确禁止**:
- 引入后续动作
- 引入最终 goal 状态

#### ✅ 优化方案 3：Scene Graph 使用策略调整

**原则**:
- ❌ 不使用 `final_sg` 做动作约束生成
- ✅ scene graph 只用于：
  - 校验（constraint checking）
  - 失败分析（why failed）
  - 物体属性补充（isOpen / isFilled）

**推荐最小方案**:
- 约束生成阶段：不依赖 scene graph
- 约束验证阶段：用 scene graph

#### ✅ 优化方案 4：删除或极简 Action Binding

**优化后逻辑**:
```python
constraint = {
    "type": "precondition",
    "template": "holding(Mug)",
    "action_index": i,  # 生成时就绑定
    "action": actions[i]
}
```

- 不再需要字符串匹配
- 不再需要语义回溯

---

## 十二、改进后的流程（推荐实现）

### 12.1 改进后的算法流程

```python
Algorithm GenerateActionAwareConstraints_Improved:
Input: action_sequence
Output: action_bound_constraints

BEGIN
    constraints = []
    FOR i, action IN enumerate(action_sequence):
        # 1. 解析当前动作
        action_type, action_args = parse_action(action)
        
        # 2. 获取动作模板
        template = ACTION_TEMPLATE_LIBRARY.get(action_type)
        
        # 3. 为当前动作生成约束（不依赖 scene graph）
        FOR each pre_template IN template["pre"]:
            constraint = instantiate_constraint(
                pre_template, 
                action_args, 
                type='precondition',
                action_index=i,
                action=action
            )
            constraints.append(constraint)
        
        FOR each post_template IN template["post"]:
            constraint = instantiate_constraint(
                post_template,
                action_args,
                type='postcondition',
                action_index=i,
                action=action
            )
            constraints.append(constraint)
    
    RETURN constraints
END
```

### 12.2 改进后的 Prompt 设计

**System Prompt**:
```
You are a robot task analyzer. Generate constraints for a SINGLE action by 
instantiating the provided action semantic template.
```

**User Prompt** (Action-local):
```
Current Action: {action}
Action Index: {action_index}
Action Type: {action_type}
Action Arguments: {action_args}

Action Semantic Template:
    Preconditions: {pre_templates}
    Postconditions: {post_templates}

Rules:
- Generate constraints ONLY for this action
- Do NOT reference future actions
- Do NOT reference final goal state
- Instantiate templates with actual object names from action arguments

Output JSON format:
{
  "constraints": [
    {
      "id": "C1",
      "type": "precondition",
      "template": "holding(Mug)",
      "description": "Robot must be holding the Mug",
      "action_index": {action_index},
      "action": "{action}"
    },
    ...
  ]
}
```

### 12.3 关键改进点总结

1. **按动作生成**: 每个动作独立生成约束，避免因果混乱
2. **生成即绑定**: 约束生成时就绑定 `action_index`，无需后续匹配
3. **不依赖 final_sg**: 约束生成阶段不使用最终场景图
4. **Action-local Prompt**: Prompt 只包含当前动作信息，不包含全任务上下文

---

## 十三、总结

### 当前实现（需要改进）

Demo3 的约束生成流程采用了 **Action-aware Constraint Generation** 方法，具有以下特点：

1. **动作级约束**: 为每个动作生成 Precondition 和 Postcondition
2. **模板化**: 使用动作语义模板指导 LLM 生成约束
3. **智能绑定**: 通过多策略匹配将约束绑定到具体动作
4. **结构化输出**: 要求 LLM 返回 JSON 格式，便于后续处理
5. **容错性**: 支持 JSON 解析失败时的文本解析回退

**但存在系统性错误**:
- 使用 final_scene_graph 导致因果顺序混乱
- 任务级生成导致约束绑定错误
- 复杂的 Action Binding 逻辑不可靠

### 改进方向（推荐实现）

采用 **Action-centric Constraint Instantiation** 方法：

1. **按动作生成**: 每个动作独立生成约束，天然绑定到 `action_index`
2. **Action-local Prompt**: Prompt 只包含当前动作，不包含全任务上下文
3. **不依赖 scene graph**: 约束生成阶段不使用 final_sg，只用于验证
4. **简化绑定**: 生成即绑定，无需复杂的匹配逻辑

这种方法确保了约束的准确性和可执行性，避免了因果顺序混乱，为后续的约束验证和失败检测提供了坚实的基础。

