# 真实环境中的感知实例化

## 3.1 引言

在真实机器人环境中，感知实例化面临着与模拟环境不同的挑战。真实环境的感知数据存在噪声、遮挡和动态变化等复杂因素，传统的封闭词汇表检测器无法满足开放世界任务的需求。本章节描述了如何将 DETIC（开放词汇表目标检测）与 CLIP（对比语言-图像预训练）相结合，实现真实环境中的零样本感知实例化，为 CRAFT 框架提供可靠的场景图构建基础。

## 3.2 DETIC+CLIP 集成方法

### 3.2.1 方法概述

本文采用 DETIC+CLIP 的零样本学习方法（Zero-shot Learning）实现自定义词汇表的对象检测。该方法的核心思想是：使用 CLIP 的文本编码器将用户定义的对象描述转换为嵌入向量，直接替换 DETIC 分类器的权重，从而实现无需训练的零样本检测能力。

### 3.2.2 DETIC 模型初始化

DETIC（Detection Transformer with In-Context Learning）是一个基于 Detectron2 框架构建的开放词汇表目标检测模型。其初始化和配置过程如下：

首先，加载 DETIC 的配置文件并初始化模型。模型使用 Swin-B 作为骨干网络，并在 L-COCO+I21k 数据集上预训练，同时使用 CLIP 的视觉编码器进行增强。模型配置从 `configs/Detic_LCOCOI21k_CLIP_SwinB_896b32_4x_ft4x_max-size.yaml` 加载，预训练权重从公开的模型仓库下载。

模型初始化过程中，需要特别处理 CUDA 兼容性问题。当检测到 CUDA 设备不可用时，自动将设备切换到 CPU 模式，确保系统在不同硬件配置下的可用性。

### 3.2.3 自定义词汇表设置

为了支持任务特定的对象检测，系统允许用户定义自定义词汇表。词汇表通常从任务信息（task_info）中获取，包含任务所需检测的所有对象描述。例如，在"制作咖啡"任务中，自定义词汇表可能包含 `['coffee machine', 'purple cup', 'blue cup with handle', 'table on the left of sink']` 等对象描述。

自定义词汇表的设置包括以下步骤：

1. **获取对象列表**：从任务信息中提取 `object_list` 字段，如果不可用，则使用默认词汇表。

2. **创建元数据**：为自定义词汇表创建 Detectron2 的 MetadataCatalog 条目，设置 `thing_classes` 属性为自定义对象列表。

3. **生成 CLIP 嵌入**：使用 CLIP 的文本编码器将每个对象描述转换为嵌入向量。嵌入生成过程使用标准的 CLIP 提示模板（如 "a {object_name}"），确保与 CLIP 预训练时的文本格式一致。

4. **替换分类器权重**：使用 `reset_cls_test()` 函数将 DETIC 分类器的权重替换为 CLIP 生成的嵌入向量。这一步骤是零样本学习的关键，它使得模型能够直接检测用户定义的对象，而无需额外训练。

5. **调整检测阈值**：对于自定义词汇表，检测阈值通常设置为 0.3（相比于默认的 0.5），以提高对真实场景中对象的召回率。

### 3.2.4 CLIP 嵌入生成与分类器替换

CLIP 嵌入生成是感知实例化的核心步骤。具体实现如下：

```python
def get_clip_embeddings(vocabulary, prompt='a '):
    """Generate CLIP embeddings for custom vocabulary"""
    text_encoder = build_text_encoder(pretrain=True)
    text_encoder.eval()
    texts = [prompt + x for x in vocabulary]
    emb = text_encoder(texts).detach().permute(1, 0).contiguous().cpu()
    return emb
```

该函数接受词汇表列表和提示模板作为输入，使用 CLIP 的文本编码器生成每个对象描述的嵌入向量。生成的嵌入向量维度为 `[embedding_dim, num_classes]`，其中 `embedding_dim` 为 CLIP 的嵌入维度（通常为 512），`num_classes` 为自定义词汇表的类别数量。

替换分类器权重时，使用 `reset_cls_test()` 函数将 DETIC 模型的分类层替换为新的嵌入向量。这一操作确保了模型能够直接输出自定义类别，而无需后处理步骤。

### 3.2.5 对象检测包装器

为了与 CRAFT 框架的接口兼容，实现了 `DeticPredictorWrapper` 类，封装了 DETIC 检测器的功能。该包装器的主要职责包括：

1. **图像格式转换**：将输入的 PIL Image 或 numpy 数组转换为 DETIC 所需的 BGR 格式。

2. **对象检测**：调用 DETIC 模型进行对象检测，过滤低置信度检测结果，并提取边界框、分割掩码等信息。

3. **深度信息融合**：当提供深度图时，计算每个检测对象的 3D 位置（基于相机内参和深度值）。

4. **调试信息输出**：对于低置信度的候选检测，输出调试信息，帮助用户理解检测结果。

包装器的 `detect_objects()` 方法返回标准化的检测结果列表，每个检测结果包含：
- `bbox`：边界框坐标 [x1, y1, x2, y2]
- `confidence`：检测置信度分数
- `label`：对象类别名称（来自自定义词汇表）
- `class_id`：类别 ID
- `mask`：分割掩码（如果可用）
- `position_3d`：3D 位置（如果提供深度信息）

### 3.2.6 方法对比与选择

本文实现了三种不同的感知实例化方法，并进行了对比分析：

**方法 A1：DETIC + CLIP 嵌入替换（零样本学习）**

这是本文采用的主要方法。通过 CLIP 嵌入直接替换 DETIC 分类器，实现了端到端的零样本检测。该方法的优点包括：
- 直接输出自定义类名，无需后处理
- 充分利用 DETIC 的区域提案能力和 CLIP 的语义理解能力
- 检测精度高，对自然语言描述的适应性好

**方法 A2：DETIC + CLIP 后处理匹配**

该方法首先使用 DETIC 检测 LVIS 数据集中的预定义类别，然后使用 CLIP 将检测结果匹配到自定义对象描述。该方法的主要缺点是两步处理过程中可能丢失信息，且需要维护 LVIS 类别的映射关系。

**REFLECT 方法：MDETR + CLIP 验证**

该方法使用 MDETR（Multimodal DETR）进行检测，然后使用 CLIP 对裁剪的区域进行验证。该方法的主要优点是通过区域裁剪和 CLIP 验证降低了误检率，但检测速度较慢，不适合实时应用。

综合考虑检测精度、速度和实现复杂度，本文选择方法 A1 作为主要的感知实例化方法。

## 3.3 真实环境的适配

### 3.3.1 检测阈值调整

真实环境中的对象检测面临光照变化、遮挡、模糊等挑战，使得检测置信度分布与训练数据不同。为了适应真实环境，本文采用了以下策略：

1. **降低检测阈值**：将默认阈值从 0.5 降低到 0.3，以提高对真实场景中对象的召回率。

2. **候选检测分析**：对于置信度在 0.2 到 0.3 之间的候选检测，输出调试信息，帮助用户理解检测过程。

3. **缺失对象处理**：当某个目标对象未被检测到时，输出缺失对象列表和可能的原因，包括对象不可见、阈值过高、名称不匹配等。

### 3.3.2 深度信息融合

真实机器人系统通常配备 RGB-D 相机，能够同时获取彩色图像和深度信息。本文利用深度信息计算检测对象的 3D 位置，为后续的空间关系计算提供基础。

3D 位置计算过程如下：
1. 提取检测边界框的中心点坐标
2. 从深度图中读取中心点位置的深度值
3. 使用相机内参将像素坐标转换为 3D 世界坐标：
   - x_3d = (u - cx) * d / fx
   - y_3d = (v - cy) * d / fy
   - z_3d = d
   
   其中 (fx, fy, cx, cy) 为相机内参，d 为深度值。

### 3.3.3 分割掩码提取

DETIC 模型不仅输出边界框，还提供像素级的分割掩码。这些掩码对于后续的点云提取和空间关系计算至关重要。

分割掩码的使用包括：
1. **点云提取**：使用掩码从深度图中提取对象对应的点云，用于精确的 3D 空间关系计算。
2. **对象区域隔离**：掩码可以用于隔离对象区域，避免背景干扰。
3. **遮挡检测**：通过掩码面积的变化，可以推断对象是否被遮挡。

## 3.4 实验验证

### 3.4.1 检测效果

在 REFLECT 数据集的真实机器人执行场景中，方法 A1 成功检测到了任务相关的对象，包括杯子、咖啡机等常见物体。检测结果显示了该方法在真实环境中的有效性。

然而，也存在一些限制：
1. 某些静态对象（如桌子、咖啡机）在某些帧中未被检测到，可能是由于视角变化或遮挡。
2. 复杂描述的对象（如 "table on the left of sink"）检测成功率较低，建议简化为核心名词（如 "table"）。

### 3.4.2 性能分析

方法 A1 的主要优势在于：
- **零样本能力**：无需针对特定任务进行模型训练，即可检测自定义对象。
- **语义理解**：CLIP 的嵌入空间捕获了语义信息，能够处理同义词和相似描述。
- **实时性**：检测速度满足实时应用需求。

主要限制包括：
- **依赖描述质量**：检测效果很大程度上取决于对象描述的准确性和详细程度。
- **阈值敏感性**：检测阈值的选择需要在精确率和召回率之间平衡。
- **背景干扰**：复杂背景可能影响检测精度。

## 3.5 总结

本章节描述了在真实环境中实现感知实例化的方法，通过 DETIC+CLIP 的零样本学习实现了自定义词汇表的对象检测。该方法充分利用了 DETIC 的区域检测能力和 CLIP 的语义理解能力，为 CRAFT 框架提供了可靠的感知基础。实验验证表明，该方法在真实机器人场景中能够有效检测任务相关的对象，为后续的场景图构建和约束验证提供了准确的信息源。
