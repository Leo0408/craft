# 边界框生成与物体界定说明

## 📋 边界框生成流程

### 1. **检测器生成原始边界框**

#### MDETR 检测器 (`mdetr_detector.py`)
- **输入**：RGB 图像 (PIL Image) + object_list（要检测的物体列表）
- **处理方式**：对 `object_list` 中的**每个物体单独调用检测**
- **输出格式**：`[x1, y1, x2, y2]`（像素坐标，左上角和右下角）
- **可能的问题**：
  - 如果同一个物体有多个检测结果（例如 "coffee machine" 被检测到多次），所有结果都会有相同的 `label`
  - 每个检测结果都有独立的 `bbox` 和 `confidence`

```python
# MDETR 检测流程
for obj_name in object_list:
    outputs = model(img, [obj_name])  # 单独检测每个物体
    # 可能返回多个检测结果（如果 keep.sum() > 1）
    for bbox, prob, mask in zip(bboxes_scaled, probas[keep], masks):
        detection = {
            'label': obj_name,  # 所有检测结果都有相同的 label
            'bbox': bbox.tolist(),  # [x1, y1, x2, y2]
            'confidence': prob
        }
```

#### DETIC 检测器 (`detic_clip_detector.py`)
- **输入**：RGB 图像 + object_list
- **处理方式**：先检测所有物体，然后通过 CLIP 过滤匹配到 object_list
- **输出格式**：`[x1, y1, x2, y2]`（像素坐标）
- **Label 匹配**：通过 CLIP 语义相似度匹配检测结果到 object_list 中的物体

```python
# DETIC 检测流程
instances = detic_model(img_array)  # 检测所有物体
for instance in instances:
    bbox = instance.pred_boxes  # [x1, y1, x2, y2]
    class_name = instance.pred_class_names  # 检测到的类别名称
    
# 然后通过 CLIP 过滤，匹配到 object_list
detections = filter_with_clip(detections, object_list)
```

### 2. **边界框存储到 Scene Graph** (`reflect_scene_graph_builder.py`)

#### ⚠️ **修复前的问题**：
```python
# 旧代码（有问题）
for det in detections:
    label = det['label']
    # ... 处理点云 ...
    bbox2d_dict[label] = np.array(det['bbox'])  # ❌ 后面的会覆盖前面的！
```

**问题**：如果同一个 `label` 有多个检测结果，后面的检测会覆盖前面的边界框，导致最终 scene graph 中的边界框可能不是置信度最高的那个。

#### ✅ **修复后的代码**：
```python
# 新代码（已修复）
# 1. 先按 label 分组，保留置信度最高的检测结果
detections_by_label = {}
for det in detections:
    label = det['label']
    confidence = det.get('confidence', 0.0)
    
    if label not in detections_by_label:
        detections_by_label[label] = det
    elif confidence > detections_by_label[label].get('confidence', 0.0):
        # 保留置信度更高的检测结果
        detections_by_label[label] = det

# 2. 处理每个唯一的检测结果
for label, det in detections_by_label.items():
    # ... 处理点云 ...
    bbox2d_dict[label] = np.array(det['bbox'])  # ✅ 确保是置信度最高的
```

### 3. **边界框格式**

- **格式**：`[x1, y1, x2, y2]`（像素坐标）
  - `x1, y1`：左上角坐标
  - `x2, y2`：右下角坐标
- **坐标系统**：
  - 原点在图像左上角
  - X 轴向右，Y 轴向下
  - 与 PIL Image 和 matplotlib 的坐标系统一致

## 🔍 物体界定流程

### 1. **物体识别**

物体通过 `det['label']` 界定：

- **MDETR**：`label` 直接来自 `object_list`（例如 "coffee machine"）
- **DETIC**：`label` 是检测到的类别名称，然后通过 CLIP 匹配到 `object_list` 中的物体

### 2. **Label 匹配逻辑**

#### MDETR
```python
# 直接使用 object_list 中的名称
for obj_name in object_list:
    detections.append({
        'label': obj_name,  # 直接使用
        'bbox': bbox,
        'confidence': prob
    })
```

#### DETIC + CLIP
```python
# 1. DETIC 检测所有物体
instances = detic_model(img)
# 2. CLIP 过滤，匹配到 object_list
for det in detections:
    for obj_name in object_list:
        if semantic_similarity(det['label'], obj_name) > threshold:
            det['matched_object'] = obj_name
            break
```

### 3. **Scene Graph 节点创建**

```python
# 在 reflect_scene_graph_builder.py 中
for label in pcd_dict.keys():
    node = Node(
        name=label,  # 使用检测到的 label
        object_type=label.split("-")[0],  # 提取基础类型
        attributes={
            'bbox2d': bbox2d_dict[label],  # 存储 2D 边界框
            'bbox3d': bbox3d,  # 存储 3D 边界框
            'pcd': obj_points  # 存储点云
        }
    )
```

## 🐛 常见问题

### 1. **边界框位置不正确**

**可能原因**：
- 检测器返回了错误的边界框（检测错误）
- 多个检测结果中，选择了置信度较低的（已修复）
- Label 匹配错误，边界框被分配给了错误的物体

**检查方法**：
```python
# 在 Step 6 中添加调试输出
print(f"  Detection for {label}: bbox={det['bbox']}, conf={det['confidence']}")
```

### 2. **边界框格式问题**

**可能原因**：
- 坐标顺序不对（应该是 [x1, y1, x2, y2]）
- 归一化坐标未转换（应该是像素坐标）

**检查方法**：
```python
# 在 Step 7 中检查
bbox2d = node.attributes.get('bbox2d')
print(f"  bbox2d type: {type(bbox2d)}, value: {bbox2d}")
print(f"  Image size: {img_w} x {img_h}")
```

### 3. **多个检测结果覆盖**

**已修复**：现在会保留置信度最高的检测结果，而不是简单地覆盖。

## 📊 调试建议

1. **在 Step 6 中添加调试输出**：
   ```python
   print(f"  📊 Detections summary:")
   for det in detections:
       print(f"    - {det['label']}: bbox={det['bbox']}, conf={det.get('confidence', 0.0):.3f}")
   ```

2. **在 Step 7 中检查边界框**：
   ```python
   print(f"  📊 Scene Graph bbox2d:")
   for node in scene_graph.nodes:
       bbox2d = node.attributes.get('bbox2d')
       print(f"    - {node.get_name()}: {bbox2d}")
   ```

3. **可视化原始检测结果**：
   - 在 Step 6 中，可以直接可视化检测器返回的原始边界框
   - 与 scene graph 中的边界框对比，找出差异

## ✅ 修复总结

**主要修复**：
- ✅ 修复了多个检测结果覆盖的问题
- ✅ 现在保留置信度最高的检测结果
- ✅ 添加了详细的调试输出

**下一步**：
- 如果边界框仍然不正确，可能是检测器本身的问题
- 建议检查检测器的原始输出，确认边界框是否正确

