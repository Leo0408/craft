# DETIC模块测试和调试指南

## 问题：注册冲突（build_mnv2_backbone already registered）

这是Jupyter notebook环境中常见的问题，因为模块可能被多次加载。

## 解决方案

### 方案1：重启Jupyter Kernel（推荐）

1. 在Jupyter notebook中：
   - 点击菜单：`Kernel → Restart Kernel`
   - 确认重启

2. 重新运行所有cells（按顺序）：
   - 从Cell 1开始
   - 依次运行到Cell 5 (Step 4)

### 方案2：在终端测试

如果重启kernel后仍然有问题，可以在终端运行测试脚本：

```bash
# 1. 激活环境
source /home/fdse/anaconda3/etc/profile.d/conda.sh
conda activate reflect_env
cd /home/fdse/zzy/craft

# 2. 运行测试脚本
python test_detic_notebook_fix.py
```

测试脚本会：
- 清理模块缓存
- 逐步测试每个导入步骤
- 显示详细的错误信息
- 提供修复建议

### 方案3：使用CLIP-only模式（备用方案）

如果DETIC加载失败，检测器会自动切换到CLIP-only模式：
- ✅ 仍可正常工作
- ⚠️ 检测精度略低于完整DETIC
- ✅ 不需要DETIC依赖

## 当前代码改进

代码已经改进了错误处理：
- 检测到注册冲突时，会尝试从缓存获取模块
- 如果缓存中也没有，会给出明确的错误提示
- 如果DETIC完全无法加载，会自动切换到CLIP-only模式

## 验证成功标志

**DETIC成功加载**：
```
✅✅✅ DETIC模型加载成功！
✅ DETIC可以正常使用！
```

**CLIP-only模式（也可以工作）**：
```
⚠️  DETIC未加载，但CLIP-only模式可用
⚠️  精度会略有下降，但仍可检测对象
```

## 如果问题仍然存在

1. 确保使用了最新的代码（git pull）
2. 检查环境是否正确激活
3. 尝试完全关闭Jupyter，重新打开
4. 检查是否有多个Python进程在使用相同的模块
