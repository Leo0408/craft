# 检测问题诊断指南

## 🔍 为什么检测不到对象？

如果优化后仍然检测不到对象，请按以下步骤诊断：

### 步骤1: 确认模块已重新加载

**问题**：代码更新后，Python可能还在使用缓存的旧版本代码。

**解决方案**：
1. **重启Kernel**（推荐）：
   - 在Jupyter Notebook中：`Kernel` → `Restart Kernel`
   - 然后重新运行所有cell（从第一个cell开始）

2. **或者强制重新加载模块**：
   - 已优化的代码在Step 4中会强制清除缓存并重新加载
   - 查看输出中是否有 "Removed from cache" 和 "Reloaded MDETRDetector module (with optimizations)"

### 步骤2: 查看详细的调试输出

优化后的代码会打印详细的调试信息。**如果看不到以下输出，说明模块没有正确加载**：

```
🔍 Detecting 'coffee machine' with X prompt variations: [...]
    ✅ Prompt 'coffee machine': found X detections, max_conf=0.XXX
    ⚠️  Prompt 'a coffee machine': max_conf=0.XXX, using lowered threshold...
    ❌ Prompt 'cup': max_conf=0.XXX < threshold...
  ✅ Best detection for 'coffee machine': confidence=0.XXX, prompt='...'
```

**如果没有看到这些输出**：
- 模块没有重新加载
- 或者检测过程中发生了异常（会被捕获并继续）

### 步骤3: 检查对象名称

MDETR对对象名称很敏感。尝试使用更简单的名称：

**原始名称** → **建议尝试的名称**
- `'coffee machine'` → `'machine'` 或 `'coffee maker'`
- `'purple cup'` → `'cup'` 或 `'mug'`
- `'blue cup with handle'` → `'cup'` 或 `'mug'`
- `'table on the left of sink'` → `'table'`

### 步骤4: 检查图像中是否真的有对象

1. 查看可视化图像（Step 7的输出）
2. 确认对象确实在图像中
3. 确认对象清晰可见（不是被遮挡或模糊）

### 步骤5: 手动测试检测器

在notebook中添加测试cell：

```python
# 手动测试检测器
from PIL import Image
import numpy as np

# 获取第一帧图像
test_frame_idx = 0
rgb_image = frame_data[test_frame_idx]['rgb']
rgb_pil = Image.fromarray(rgb_image)

# 测试简化的对象名称
test_objects = ['cup', 'machine', 'table']  # 使用简单的名称

print("Testing detector with simplified names...")
detections = detector.detect_objects(rgb_pil, test_objects)
print(f"Found {len(detections)} detections")
for det in detections:
    print(f"  - {det['label']}: confidence={det['confidence']:.3f}")
```

### 步骤6: 检查MDETR模型是否正确加载

```python
# 检查模型状态
print(f"Model loaded: {detector.model is not None}")
print(f"Device: {detector.device}")
print(f"Threshold: {detector.threshold}")
```

如果`detector.model is None`，说明模型没有正确加载。

### 步骤7: 尝试降低阈值

在Step 4初始化检测器后，手动降低阈值：

```python
# 初始化后降低阈值
detector.threshold = 0.3  # 或更低，如 0.2, 0.1
print(f"Threshold lowered to: {detector.threshold}")
```

### 步骤8: 检查是否有异常被静默

优化后的代码会捕获异常并打印。检查输出中是否有：
- `⚠️  Error with prompt '...': ...`
- `Traceback` 信息

如果有异常，说明检测过程中出现了错误。

## 🎯 常见问题

### Q1: 为什么优化后还是检测不到？

**A**: 最可能的原因是：
1. 模块没有重新加载（需要重启kernel）
2. 对象名称与MDETR训练数据不匹配
3. 图像中对象确实不存在或不可见
4. MDETR模型本身对该场景/对象的检测能力有限

### Q2: 如何确认优化代码是否生效？

**A**: 查看输出中是否有：
- `🔍 Detecting '...' with X prompt variations: ...`
- 每个提示词的检测结果（✅ 或 ❌）
- 最终选择的检测结果

如果没有这些输出，说明优化代码没有生效。

### Q3: 我应该使用MDETR还是DETIC+CLIP？

**A**: 
- **MDETR**：快速测试，如果对象名称与MDETR训练数据匹配
- **DETIC+CLIP**：更高精度，对对象名称变化更鲁棒，但需要更多依赖

如果MDETR检测不到，建议尝试DETIC+CLIP（即使DETIC不可用，也会自动切换到CLIP-only模式）。

## 📝 下一步建议

1. **重启Kernel并重新运行所有cell**
2. **查看详细调试输出**，确认优化代码生效
3. **如果仍然检测不到，尝试**：
   - 使用简化的对象名称
   - 切换到DETIC+CLIP方法
   - 检查图像中是否真的有对象
   - 手动测试检测器

