# DETIC注册冲突问题分析和修复方案

## 问题根本原因

注册冲突 `build_mnv2_backbone already registered` 的根本原因是**导入顺序问题**：

1. **adet.modeling先被导入**：
   - 在`detic_clip_detector.py`第260行，先导入`adet.modeling`
   - `adet.modeling`导入时会注册`build_mnv2_backbone`到BACKBONE注册表

2. **detic.config后被导入**：
   - 在`detic_clip_detector.py`第286行，导入`from detic.config import add_detic_config`
   - `detic/__init__.py`会导入`.modeling.backbone`
   - 这会导入`centernet.modeling.backbone.mobilenet`
   - `mobilenet.py`会尝试再次注册`build_mnv2_backbone`
   - **冲突发生**！

## 解决方案

### 方案1：调整导入顺序（推荐）

**不要在导入`detic.config`之前导入`adet.modeling`**

修改`detic_clip_detector.py`，将adet.modeling的导入移到detic.config导入之后：

```python
# 错误的顺序（当前）：
import adet.modeling  # 注册了build_mnv2_backbone
from detic.config import add_detic_config  # 尝试再次注册，冲突！

# 正确的顺序：
from detic.config import add_detic_config  # 先导入detic
# 然后再导入adet.modeling（如果需要）
import adet.modeling
```

### 方案2：使用直接文件加载（已在代码中实现）

代码已经尝试了直接文件加载的方式，但需要确保在遇到注册冲突时正确使用。

### 方案3：在全新环境中运行（最简单）

在Jupyter notebook中：
1. **完全重启kernel**（`Kernel → Restart Kernel`）
2. **重新运行所有cells（按顺序）**
3. 确保没有在其他地方提前导入`adet.modeling`

## 测试步骤

在终端运行以下命令来验证修复：

```bash
source /home/fdse/anaconda3/etc/profile.d/conda.sh
conda activate reflect_env
cd /home/fdse/zzy/craft
python test_detic_step_by_step.py
```

## 推荐的代码修改

修改`perception/detic_clip_detector.py`中的导入顺序：

1. 先导入`detic.config`（不要先导入`adet.modeling`）
2. 然后导入`adet.modeling`用于注册CenterNet
3. 最后导入`detic.modeling`用于注册CustomRCNN

这样可以避免注册冲突。

